install:
 # tex install
 - source ./.ci-support/install_tex_packages.sh
cache:
  directories:
    - /tmp/texlive
    - $HOME/.texlive
script:
 - pdflatex -shell-escape source/thesis.tex
 - bibtex source/thesis
 - pdflatex -shell-escape source/thesis.tex
 - pdflatex -shell-escape source/thesis.tex
