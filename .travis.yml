install:
 # tex install
 - source ./.ci-support/install_tex_packages.sh
cache:
  directories:
    - /tmp/texlive
    - $HOME/.texlive
script:
 - cd source
 - pdflatex -shell-escape thesis.tex
 - bibtex thesis
 - pdflatex -shell-escape thesis.tex
 - pdflatex -shell-escape thesis.tex
